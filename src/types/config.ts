// import { Player } from "./players.ts";

export type GameConfig = {
  minBet: number;
  autoTime: number;
  deckCount: number;
  playerCount: number;
};

/*
export type NewGameOptions = {
  players: Player[];
  config: GameConfig;
};
*/
